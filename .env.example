# Copy this file to .env and fill in real values. Never commit .env.

# Absolute or PATH-resolvable path to the Python interpreter the Next.js
# backend should spawn for extraction.
PYTHON_BIN="python3"

# Max concurrent PDF-parsing worker processes (see scripts/parser.py). Leave
# unset to default to the machine's CPU count.
PARSER_MAX_WORKERS=""
